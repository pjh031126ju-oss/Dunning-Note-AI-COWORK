# Dunning Note AI / 더닝노트

PARA 기반으로 메모를 직접 분류하고, 프로젝트 작업과 주간 리뷰를 로컬에서 관리하는 Next.js 프로토타입입니다. AI API, 캘린더 연동, 백엔드 데이터베이스 없이 `localStorage`만 사용합니다.

## Run locally

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:3000`을 열면 됩니다.

## Scripts

```bash
npm run lint
npm run build
```
